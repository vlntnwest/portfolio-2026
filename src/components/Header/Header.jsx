import projects from "@/lib/projects.json";
import MenuLink from "../ui/buttons/MenuLink";

const Header = () => {
  return (
    <header>
      <div></div>
    </header>
  );
};

export default Header;
