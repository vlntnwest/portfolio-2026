import ProjectsWrapper from "@/components/Project/ProjectsWrapper";

const Home = () => {
  return (
    <main className="h-screen relative">
      <ProjectsWrapper />
    </main>
  );
};

export default Home;
